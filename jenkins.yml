version: "3.5"
services: 
  wordpress:
    image: jenkins/jenkins:lts
    deploy:
      replicas: 2
      resources:
        reservations:
          cpus: '2'
          memory: 2048
      # placement:
      #   constraints: 
      #     - "node.labels.zone != 1"
      #   preferences:
      #     - spread: node.labels.zone
    ports:
      - "80:80"
